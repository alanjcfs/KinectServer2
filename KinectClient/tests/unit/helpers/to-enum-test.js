import { toEnum } from 'kinect-client/helpers/to-enum';
import { module, test } from 'qunit';

module('Unit | Helper | to enum');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toEnum([42]);
  assert.ok(result);
});
