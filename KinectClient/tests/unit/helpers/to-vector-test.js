import { toVector } from 'kinect-client/helpers/to-vector';
import { module, test } from 'qunit';

module('Unit | Helper | to vector');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toVector([42]);
  assert.ok(result);
});
